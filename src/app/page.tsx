'use client';

import { DemoContent } from '@/components/demo/DemoContent';

export default function Home() {
  return (
    <main className="mx-auto w-full max-w-5xl flex-1 px-4 py-4 sm:px-6 sm:py-8 lg:px-8">
      <DemoContent />
    </main>
  );
}
